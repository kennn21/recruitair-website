export type Job = {
    title: string,
    description: string,
    imageUrl: string,
    salary: string,
    startDate: Date,
    endDate: Date,
    createdAt: Date,
    createdBy: string,
    updatedAt: Date,
    updatedBy: string,
}